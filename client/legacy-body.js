/*
import { Template } from 'meteor/templating';

import { SimpleSchema } from 'meteor/aldeed:simple-schema';

import { Teams } from '../api/players.js';
import { Players } from '../api/players.js';
import { Schemas } from '../api/players.js';

import './body.html';
import './team.html';
import './addTeam.html';

Template.body.helpers({
  teams() {
    return Teams.find({});
  }
  //players() {
 	//	return Players.find({});
 //	}
});

Template.team.helpers({
  //This brings teams to the body
  teams () {
    return Teams.find({});
  }
});

Template.addTeam.helpers({
 	Teams() {
 		return Teams.find({});
 	}
});
*/
